name: Taxonomy

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: README table matches patterns/
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python3 rules/check_taxonomy.py
